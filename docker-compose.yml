version: '3.9'
services:
  bot:
    build: .
    container_name: tg-plus-bot
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      DB_PATH: /data/data.db
    volumes:
      - ./data:/data
    restart: unless-stopped